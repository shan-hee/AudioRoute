<Window
    x:Class="AudioRoute.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="AudioRoute">
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="WindowRoot" Background="Transparent" RequestedTheme="Dark">
        <Grid.Resources>
            <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F12B2B2B" />
            <SolidColorBrush x:Key="PanelBorderBrush" Color="#1AFFFFFF" />
        </Grid.Resources>

        <Border x:Name="PanelSurface"
                Padding="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="12">
            <ScrollViewer Margin="8,8,0,8"
                          VerticalScrollBarVisibility="Visible"
                          HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="SessionHost" Spacing="2" Padding="0,4,8,4" />
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
